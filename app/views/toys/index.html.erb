

<section class="homehero">

<p>
<button class="button"> <%= link_to 'Add New Toy', new_toy_path %></button>
</p>

<% @toys.each do |toy| %>
    
	<div class="box">
   		<div class="innerbox">
   				<div class='<%= "landscape" if toy.notportrait? %>'>
       			<%= link_to (image_tag (toy.photo.url)), toy %>
       			</div>
   			<div class="titleBox">
   				<%= toy.name%>
   			</div>
   		</div>
	</div>



<%end%>

</section>


