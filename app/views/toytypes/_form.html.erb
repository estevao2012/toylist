<section class="showcontent">
<h1>
 Toy Types Details
</h1>

<%= form_for(@toytype) do |f| %>
  <% if @toytype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@toytype.errors.count, "error") %> prohibited this toytype from being saved:</h2>

      <ul>
      <% @toytype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :toytype, :placeholder =>"Toy Type" %>
  </div>
  <div class="field">
    <%= f.text_field :description, :placeholder => "Description" %>
  </div>

  <div class="field">
    <%= f.submit "Save" , :class => "button" %>
  </div>
<% end %>


</section>